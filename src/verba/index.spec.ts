import { verba } from '.'

describe('verba', () => {
  describe('verba', () => {
    const fn = verba

    test('basic test', () => {
      const instance = fn()
      expect(instance).toBeDefined()
    })
  })
})
